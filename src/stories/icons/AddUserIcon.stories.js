import React from 'react';
import AddUserIcon from '../../components/icons/AddUserIcon';

export default {
  title: 'Icons/AddUser',
  component: AddUserIcon,
};

const Template = args => <AddUserIcon {...args} />;

export const Default = Template.bind({});

Default.args = {};
