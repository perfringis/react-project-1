import React from 'react';
import PenIcon from '../../components/icons/PenIcon';

export default {
  title: 'Icons/Pen',
  component: PenIcon,
};

const Template = args => <PenIcon {...args} />;

export const Default = Template.bind({});

Default.args = {};
