import React from 'react';
import BoltIcon from '../../components/icons/BoltIcon';

export default {
  title: 'Icons/Bolt',
  component: BoltIcon,
};

const Template = args => <BoltIcon {...args} />;

export const Default = Template.bind({});

Default.args = {};
