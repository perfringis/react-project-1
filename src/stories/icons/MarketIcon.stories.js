import React from 'react';
import MarketIcon from '../../components/icons/MarketIcon';

export default {
  title: 'Icons/Market',
  component: MarketIcon,
};

const Template = args => <MarketIcon {...args} />;

export const Default = Template.bind({});

Default.args = {};
