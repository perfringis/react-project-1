import React from 'react';
import CompassIcon from '../../components/icons/CompassIcon';

export default {
  title: 'Icons/Compass',
  component: CompassIcon,
};

const Template = args => <CompassIcon {...args} />;

export const Default = Template.bind({});

Default.args = {};
